declare module '*.png' {
    const value: string; // Указываем, что импортируемый файл — это строка
    export default value;
  }
  
  declare module '*.jpg' {
    const value: string; // Указываем, что импортируемый файл — это строка
    export default value;
  }
  
  declare module '*.jpeg' {
    const value: string; // Указываем, что импортируемый файл — это строка
    export default value;
  }
  
  declare module '*.svg' {
    const value: string; // Указываем, что импортируемый файл — это строка
    export default value;
  }